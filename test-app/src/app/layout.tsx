export const metadata = {
  title: "VibeTasks",
  description: "Task management for vibes",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
